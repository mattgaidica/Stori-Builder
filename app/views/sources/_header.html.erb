<div class="bg-holder d-none d-lg-block bg-card" style="background-image:url(../assets/img/illustrations/corner-4.png);">
</div> <!--/.bg-holder-->
<div class="card-body">
  <div class="row">
    <div class="col-lg-12">
      <h3 class="mb-0"><%= @source.title %></h3>
      <a data-toggle="modal" data-target="#annotationModal" class="nav-link float-right" href="#"><span class="fas fa-plus"></span> New Annotation</a>
      <p class="mt-2"><%= @source.authors %></p>
  	</div>
  </div>
</div>

<% @source.entities.each do |entity| %>
  <span class="badge badge-dark">#<%= entity.content %></span>
<% end %>